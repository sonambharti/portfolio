import { useState } from "react";
import '../Style/Skills.css';
const {skillsData} = require('../data/SkillData.js');

export default function Skills (){
    return (
        <div className="skills">
        </div>
    )
}